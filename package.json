{
  "name": "insurance-policy-api",
  "version": "1.0.0",
  "description": "Node.js + MongoDB API: XLSX/CSV import via worker threads, policy search, per-user aggregation, CPU-based auto-restart, scheduled messages",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "cluster": "node src/cluster.js"
  },
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.6.0",
    "multer": "^1.4.5-lts.1",
    "node-cron": "^3.0.3",
    "os-utils": "^0.0.14",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
